# Denied Actions

The following actions are strictly prohibited:
- accessing files outside `/opt/automation` (except standard system binaries/logs)
- executing arbitrary downloaded scripts without inspection
- running commands as root/sudo unless specifically approved
- transferring private user keys or database files outside the host
